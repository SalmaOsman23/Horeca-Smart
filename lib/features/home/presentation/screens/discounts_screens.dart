import 'package:flutter/material.dart';

class DiscountsScreens extends StatelessWidget {
  const DiscountsScreens({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text("Discounts")
      ],
    );
  }
}